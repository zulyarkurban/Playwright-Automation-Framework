import { PrettyPrinter } from './PrettyPrinter.js';
import { CUCUMBER_THEME } from './theme.js';
const DEFAULT_OPTIONS = {
    attachments: true,
    featuresAndRules: true,
    theme: CUCUMBER_THEME,
};
export * from './theme.js';
export default {
    type: 'formatter',
    formatter({ on, options = {}, stream = process.stdout, write, }) {
        const printer = new PrettyPrinter(stream, write, {
            ...DEFAULT_OPTIONS,
            ...options,
        });
        on('message', (message) => printer.update(message));
    },
    optionsKey: 'pretty',
};
//# sourceMappingURL=index.js.map